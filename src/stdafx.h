#pragma once

#pragma warning(disable: 4996)	// src/corelib/tools/qlist.h:849 (when will change to: return std::equal(b, e, QT_MAKE_UNCHECKED_ARRAY_ITERATOR(lb)); it could be possible to remove)
#include <QtWidgets>
#pragma warning (default: 4996)
//#include <QGLWidget>
#include <QtXml>
#include <QRegExp>
